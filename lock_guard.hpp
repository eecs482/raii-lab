#pragma once

class lock_guard
{
    // 1. Create a constructor that accepts a reference to a mutex

    // 2. Create a destructor that unlocks the mutex

    // 3. Delete the copy constructor and copy assignment operator

    // 4. Create a move constructor and move assignment operator
};
